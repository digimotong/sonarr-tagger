[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
# Coverage flags live in the CI invocation, not here: addopts applies to every
# run, so a single-file run would trip a coverage floor it never had to meet.
