-r requirements.txt
# Pinned to match CI's interpreters (3.12, 3.13, 3.14) so local and CI resolve
# the same versions. pytest 9 requires Python >= 3.9.
pytest==9.1.1
pytest-cov==7.1.0
pylint==4.0.8

# tests/test_twin_parity.py parses .github/workflows/tests.yml rather than
# matching its text (the file's own comments name `paths:`/`branches:`, and no
# regex can see a job grow a `strategy:` block), so a YAML reader is required.
pyyaml==6.0.3
